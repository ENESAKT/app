name: Build and Release APK

on:
  push:
    branches:
      - main
    tags:
      - 'v*'
  workflow_dispatch:

jobs:
  build:
    name: Build APK
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout Repository
        uses: actions/checkout@v4

      - name: ☕ Setup Java 17
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'
          cache: 'gradle'

      - name: 📱 Setup Android SDK 36
        run: |
          yes | $ANDROID_HOME/cmdline-tools/latest/bin/sdkmanager --licenses || true
          $ANDROID_HOME/cmdline-tools/latest/bin/sdkmanager "platforms;android-36" "build-tools;36.0.0" || true

      - name: 🐦 Setup Flutter (Stable)
        uses: subosito/flutter-action@v2
        with:
          channel: 'stable'
          cache: true

      - name: 🔐 Configure Supabase Secrets
        working-directory: ./frontend
        run: |
          mkdir -p .dart_tool
          echo "SUPABASE_URL=${{ secrets.SUPABASE_URL }}" > .env
          echo "SUPABASE_ANON_KEY=${{ secrets.SUPABASE_ANON_KEY }}" >> .env

      - name: 📦 Get Flutter Dependencies
        working-directory: ./frontend
        run: flutter pub get

      - name: 🔐 Decode Release Keystore
        run: |
          echo "${{ secrets.KEYSTORE_BASE64 }}" | tr -d '\r\n\t ' | base64 --decode > frontend/android/app/upload-keystore.jks
          ls -lh frontend/android/app/upload-keystore.jks
        env:
          KEYSTORE_BASE64: ${{ secrets.KEYSTORE_BASE64 }}

      - name: 🔧 Build APK (Release)
        working-directory: ./frontend
        run: |
          flutter build apk --release \
            --dart-define=SUPABASE_URL="${{ secrets.SUPABASE_URL }}" \
            --dart-define=SUPABASE_ANON_KEY="${{ secrets.SUPABASE_ANON_KEY }}"
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_ANON_KEY: ${{ secrets.SUPABASE_ANON_KEY }}
          KEYSTORE_PASSWORD: ${{ secrets.KEYSTORE_PASSWORD }}
          KEY_ALIAS: ${{ secrets.KEY_ALIAS }}
          KEY_PASSWORD: ${{ secrets.KEY_PASSWORD }}

      - name: 📦 Upload APK Artifact
        uses: actions/upload-artifact@v4
        with:
          name: app-release-apk
          path: frontend/build/app/outputs/flutter-apk/app-release.apk
          retention-days: 30

      - name: 📋 Get Version Info
        id: version
        run: |
          if [[ "$GITHUB_REF" == refs/tags/* ]]; then
            TAG="${{ github.ref_name }}"
            VERSION="${TAG#v}"
          else
            VERSION="1.0.0-${GITHUB_SHA:0:7}"
            TAG="v${VERSION}"
          fi
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "tag=$TAG" >> $GITHUB_OUTPUT
          echo "📌 Version: $VERSION, Tag: $TAG, Build: ${{ github.run_number }}"

      - name: 🏷️ Create GitHub Release
        id: release
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v1
        with:
          tag_name: ${{ github.ref_name }}
          name: Release ${{ github.ref_name }}
          body: |
            ## 🚀 Project-Friend ${{ github.ref_name }}
            
            ### 📥 Kurulum
            1. APK dosyasını indirin
            2. Telefonunuzda "Bilinmeyen Kaynaklardan Kurulum" aktif olmalı
            3. APK'yı kurun
            
            ---
            **Build:** #${{ github.run_number }}
          files: frontend/build/app/outputs/flutter-apk/app-release.apk
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      # =====================================================
      # � SUPABASE APP_CONFIG TABLOSUNU GÜNCELLE
      # Release sonrası otomatik çalışır (sadece v* tag)
      # =====================================================
      - name: 🔄 Update Supabase app_config
        if: startsWith(github.ref, 'refs/tags/')
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_KEY: ${{ secrets.SUPABASE_SERVICE_KEY }}
          VERSION: ${{ steps.version.outputs.version }}
          TAG: ${{ github.ref_name }}
          REPO: ${{ github.repository }}
          BUILD_NUMBER: ${{ github.run_number }}
        run: |
          # APK indirme linkini oluştur
          DOWNLOAD_URL="https://github.com/${REPO}/releases/download/${TAG}/app-release.apk"
          
          echo "📦 Supabase app_config güncelleniyor..."
          echo "   Current Version: ${VERSION}"
          echo "   Build Number: ${BUILD_NUMBER}"
          echo "   Download URL: ${DOWNLOAD_URL}"
          
          # Supabase REST API ile PATCH isteği (id=1 olan satırı güncelle)
          HTTP_STATUS=$(curl -s -o /tmp/response.txt -w "%{http_code}" \
            -X PATCH "${SUPABASE_URL}/rest/v1/app_config?id=eq.ea7ba3bf-f1c5-4361-becd-d5e42c62c975" \
            -H "apikey: ${SUPABASE_KEY}" \
            -H "Authorization: Bearer ${SUPABASE_KEY}" \
            -H "Content-Type: application/json" \
            -H "Prefer: return=minimal" \
            -d "{
              \"current_version\": \"${VERSION}\",
              \"build_number\": ${BUILD_NUMBER},
              \"download_url\": \"${DOWNLOAD_URL}\",
              \"is_force_update\": false
            }")
          
          echo "� HTTP Status: ${HTTP_STATUS}"
          
          if [ "$HTTP_STATUS" -ge 200 ] && [ "$HTTP_STATUS" -lt 300 ]; then
            echo "✅ Supabase app_config başarıyla güncellendi!"
          else
            echo "❌ Supabase güncelleme hatası!"
            cat /tmp/response.txt
            exit 1
          fi