name: frontend
description: "ArkadaÅŸlÄ±k UygulamasÄ± - Flutter Frontend"
publish_to: 'none'
version: 1.0.45+1

environment:
  sdk: ^3.10.7

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  
  # HTTP ve API
  http: ^1.6.0
  
  # Firebase
  firebase_core: ^3.8.0
  firebase_auth: ^5.3.3
  firebase_messaging: ^15.1.5
  
  # Google Sign-In
  google_sign_in: ^6.2.0
  
  # Supabase - PostgreSQL + Realtime Chat
  supabase_flutter: ^2.5.0
  
  # Image Picker (profil fotoÄŸrafÄ± iÃ§in)
  image_picker: ^1.1.2
  
  # State Management
  provider: ^6.1.0
  
  # Ekran yÃ¶nlendirme
  go_router: ^14.6.0
  
  # Shared Preferences (token saklama)
  shared_preferences: ^2.3.0
  
  # Resim Ã¶nbelleÄŸi
  cached_network_image: ^3.4.0
  
  # Utilities
  timeago: ^3.7.0
  intl: ^0.19.0
  url_launcher: ^6.3.1
  
  # OTA Updates (In-App Download & Install)
  package_info_plus: ^8.0.3
  dio: ^5.4.0
  open_filex: ^4.4.0
  path_provider: ^2.1.2
  permission_handler: ^12.0.1
  
  # Super App - API Integrations
  geolocator: ^14.0.2           # Konum servisi (Weather iÃ§in)
  shimmer: ^3.0.0               # Loading efekti
  flutter_map: ^7.0.2           # Harita widget (Geoapify ile)
  latlong2: ^0.9.1              # Lat/Long koordinatlarÄ±
  google_generative_ai: ^0.4.6  # Gemini AI chatbot

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  
  # Branding - Uygulama ikonu ve splash screen
  flutter_launcher_icons: ^0.14.3
  flutter_native_splash: ^2.4.4
  change_app_package_name: ^1.4.0

flutter:
  uses-material-design: true

# Flutter Launcher Icons Configuration
flutter_launcher_icons:
  android: "launcher_icon"
  ios: true
  image_path: "assets/icon/icon.png"
  min_sdk_android: 21

# Flutter Native Splash Configuration
flutter_native_splash:
  color: "#4A148C"
  image: "assets/icon/icon.png"
  android_12: true
